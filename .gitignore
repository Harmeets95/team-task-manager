# Node modules (Never push these, they are too heavy. Servers will install them automatically)
node_modules/
frontend/node_modules/
backend/node_modules/

# Environment Variables (Never push these to keep your passwords and secrets safe!)
.env
backend/.env
frontend/.env

# Build outputs (Vercel will generate these automatically)
frontend/dist/
dist/

# OS generated files
.DS_Store
Thumbs.db

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*